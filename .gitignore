# --- cloned third-party sources (regenerated by the build scripts; not vendored) ---
evalkit/src/
evalkit/ext_src/
evalkit/new_src/

# --- build outputs: compiled corpora (regenerated by the build scripts) ---
# Remove these lines if you want to archive the exact .o objects alongside the results.
evalkit/corpus/
evalkit/corpus_ext/
evalkit/corpus_clang/

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# --- LaTeX build artifacts ---
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.synctex.gz
paper/*.fls
paper/*.fdb_latexmk
paper/*.toc

# --- OS / editor ---
.DS_Store
*.swp
